# SOT Attendance Frontend

Read `AGENTS.md` before changing code. It is the project architecture source of truth.

Useful commands:

```sh
pnpm dev
pnpm lint
pnpm test
```

Do not expose `AUTH_SECRET` or Discord credentials to client components.
